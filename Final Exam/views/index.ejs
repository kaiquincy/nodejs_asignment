<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>TreeShop</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;700&display=swap" rel="stylesheet">
  <!-- Custom CSS -->
  <style>
    body {
      background-color: #f3f4f6;
      font-family: 'Roboto', sans-serif;
    }
    .tree-card {
      border-radius: 15px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      transition: transform 0.3s ease-in-out;
      overflow: hidden;
    }
    .tree-card:hover {
      transform: scale(1.03);
    }
    .tree-card img {
      height: 200px;
      object-fit: cover;
      border-top-left-radius: 15px;
      border-top-right-radius: 15px;
    }
    .btn-custom {
      background-color: #28a745;
      color: white;
      font-weight: bold;
    }
    .btn-custom:hover {
      background-color: #218838;
    }
    .reset-btn {
      background-color: #dc3545;
      color: white;
    }
    .reset-btn:hover {
      background-color: #c82333;
    }
  </style>
</head>
<body>
  <%- include('layout/header') %>

  <div class="container my-5">
    <div class="text-center mb-4">
      <h1 class="display-4 text-success">🌳 Welcome to TreeShop 🌿</h1>
      <p class="lead">Discover and collect your favorite trees!</p>
    </div>

    <!-- Form thêm cây -->
    <div class="card mb-4">
      <div class="card-body">
        <h4 class="mb-3">Add a New Tree</h4>
        <form action="/add" method="POST" class="row g-3">
          <div class="col-md-4">
            <input type="text" name="treename" class="form-control" placeholder="Tree Name" required>
          </div>
          <div class="col-md-4">
            <input type="text" name="description" class="form-control" placeholder="Description" required>
          </div>
          <div class="col-md-3">
            <input type="text" name="image" class="form-control" placeholder="Image URL">
          </div>
          <div class="col-md-1 d-grid">
            <button type="submit" class="btn btn-custom">Add</button>
          </div>
        </form>
      </div>
    </div>

    <!-- Danh sách cây -->
    <h2 class="mb-4 text-success">🌿 Tree Collection</h2>
    <div class="row">
      <% trees.forEach(tree => { %>
        <div class="col-md-4">
          <div class="card tree-card">
            <% if (tree.image) { %>
              <img src="<%= tree.image %>" class="card-img-top" alt="Tree Image">
            <% } %>
            <div class="card-body text-center">
              <h5 class="card-title"><%= tree.treename %></h5>
              <p class="card-text"><%= tree.description %></p>
              <a href="/edit/<%= tree._id %>" class="btn btn-sm btn-outline-primary">Edit</a>
              <form action="/delete/<%= tree._id %>" method="POST" class="d-inline">
                <button type="submit" class="btn btn-sm btn-danger">Delete</button>
              </form>
            </div>
          </div>
        </div>
      <% }) %>
    </div>

    <!-- Nút Reset -->
    <div class="text-center mt-4">
      <form action="/reset" method="POST">
        <button type="submit" class="btn reset-btn">Reset All Trees</button>
      </form>
    </div>
  </div>

  <%- include('layout/footer') %>
  <!-- Bootstrap Bundle JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
